var RawSharer;
(function (RawSharer) {
    var Common;
    (function (Common) {
        var Utils;
        (function (Utils) {
            var Math = (function () {
                function Math() {
                }
                Math.binarySearch = function (data, target) {
                    var high = data.length - 1;
                    var low = 0;
                    while (low < high + 2) {
                    }
                };
                return Math;
            }());
            Utils.Math = Math;
        })(Utils = Common.Utils || (Common.Utils = {}));
    })(Common = RawSharer.Common || (RawSharer.Common = {}));
})(RawSharer || (RawSharer = {}));
//# sourceMappingURL=Math.js.map